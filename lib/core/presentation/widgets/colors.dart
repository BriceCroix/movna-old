import 'package:flutter/material.dart';

/// For everything related to start (start point, start button...).
const startColor = Colors.green;

/// For everything related to pause (pause button, pause indicator...).
const pauseColor = Colors.amber;

/// For everything related to stop (stop button, end point...).
const stopColor = Colors.red;

/// For all elements representing user position or path.
const userPositionColor = Colors.lightBlue;

/// The color of an itinerary on a map.
const itineraryPathColor = Colors.grey;
